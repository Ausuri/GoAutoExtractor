package regextools

import (
	"testing"
)

func TestSanitizeExtension(t *testing.T) {
	//sanitizer := RegexTool{}

	//Do something here like "for each extension in AllowedExtensions.txt, sanitize, and check filename with extension removed.
}

func TestVerifyValidArchive(t *testing.T) {
	//Do something here like "for each extension in AllowedExtensions.txt, verify each item in some array (or list) passes the verification.
}
